"""Batched matrix multiplication without library matmul operations."""

import numpy as np


def batched_matrix_multiply(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """Multiply matching batches with shapes (B, M, K) and (B, K, N)."""

    raise NotImplementedError
