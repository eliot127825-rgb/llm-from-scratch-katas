# 02 Tensor Operations

## 已发布

1. `001_matrix_multiplication`
2. `002_transpose_2d`
3. `003_batched_matrix_multiplication`
4. `004_causal_mask`
5. `005_stable_logsumexp`

## 待扩展

1. Indexing 与 slicing
2. Reshape、view 与 flatten
3. Broadcasting
4. `einsum`
5. Padding 与 sequence mask
6. Gather 与 scatter
7. 向量化 pairwise distance
8. 数值梯度检查

每题必须在注释中标出输入和输出 shape，并覆盖 batch size 为 1、空 mask 等边界情况。
