"""Prediction step for multivariate linear regression."""

import numpy as np


def linear_regression_predict(
    features: np.ndarray,
    weights: np.ndarray,
    bias: float,
) -> np.ndarray:
    """Return one linear prediction for every feature row."""

    raise NotImplementedError
