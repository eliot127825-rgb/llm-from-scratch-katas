"""Numerically stable softmax."""

import numpy as np


def stable_softmax(x: np.ndarray, axis: int = -1) -> np.ndarray:
    """Normalize finite floating-point logits along one axis."""

    raise NotImplementedError
